<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- the theme applied to the application or activity -->
    <style name="LockphoneActionBarTheme"
        parent="@style/Theme.AppCompat.Light.DarkActionBar">
        <item name="android:actionBarStyle">@style/LockphoneActionBar</item>

        <!-- Support library compatibility -->
        <item name="actionBarStyle">@style/LockphoneActionBar</item>
    </style>

    <!-- ActionBar styles -->
    <style name="LockphoneActionBar"
        parent="@style/Widget.AppCompat.Light.ActionBar.Solid.Inverse">
        <item name="android:background">@color/lp_red</item>

        <!-- Support library compatibility -->
        <item name="background">@color/lp_red</item>
    </style>
</resources>